# Retained fixture method

The five rows are a deliberately bounded calibration-data fixture, not a claim
about a named instrument, site, population, or physical process. Parse the
`temperature_c` column as exact decimal values, sort the five values, and take
the third value as the median. The result does not estimate uncertainty,
accuracy, representativeness, or external validity.
