{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vela.science/schemas/vela.signer-enrollment-request.v1.schema.json",
  "title": "Vela protected signer enrollment request v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "nonce",
    "expires_at",
    "vela_binary_path",
    "vela_binary_sha256",
    "helper_sha256",
    "actor",
    "public_key",
    "source_path",
    "provider",
    "protection_mode",
    "remove_source_after_install"
  ],
  "properties": {
    "schema": { "const": "vela.signer-enrollment-request.v1" },
    "nonce": { "$ref": "#/$defs/lowerHex32" },
    "expires_at": { "type": "string", "format": "date-time" },
    "vela_binary_path": { "type": "string", "minLength": 1 },
    "vela_binary_sha256": { "$ref": "#/$defs/sha256" },
    "helper_sha256": { "$ref": "#/$defs/sha256" },
    "actor": { "type": "string", "minLength": 1 },
    "public_key": { "$ref": "#/$defs/lowerHex32" },
    "source_path": { "type": "string", "minLength": 1 },
    "provider": { "type": "string", "minLength": 1 },
    "protection_mode": { "enum": ["session", "always"] },
    "remove_source_after_install": { "const": true }
  },
  "$defs": {
    "sha256": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "lowerHex32": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
  }
}
