# Continue one exact scientific Frontier

Act as a cold successor. Inspect `/workspace/frontier` and select its first
current producer Target. {{TOOL_GUIDANCE}}

Complete that Target in the exact Formal Conjectures checkout at
`/workspace/formal-conjectures`. A public predecessor proof is available at
`/workspace/reference-proof`; it is evidence for the older definition and may
need semantic repair rather than textual copying.

Write exactly one complete replacement Lean source file to
`/logs/artifacts/264.lean`. Preserve every byte outside the body of the target
theorem and preserve its exact signature. Do not modify either Git checkout.

The verifier runs native Lean in a separate, network-denied environment. It
rejects `sorry`, `admit`, new axioms, opaque or unsafe escape hatches, unrelated
source edits, and unexpected axioms.

Do not submit, verify, accept, reject, cancel, sign, publish, or otherwise
change Vela Standing. Your output is a candidate artifact, not a verdict.
