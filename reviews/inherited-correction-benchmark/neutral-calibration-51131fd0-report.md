# Independent neutral schema calibration confirmation

## Verdict

**PASS**, bound to producer evidence commit
`51131fd07d44b52c0bd550110d39d06891adaa54`, tree
`8c500ae3c6c04b3459079eda6e38314247a25a9a`, parent
`bccae8ff047d20797347361725b8510c08e56960`, and remote branch
`refs/heads/codex/inherited-correction-study` at that exact commit.

The frozen evidence establishes one and only one neutral, non-scientific
schema calibration provider call on attempt 1. The distinct calibration permit
was atomically consumed before provider start, the exact preregistered response
was returned, the full registered schema and provider derivative both accepted
it, and the container terminated cleanly. No participant session began. All 36
participant permits remain held and unconsumed.

This review made no provider call, released no permit, accessed no protected
adjudication plaintext or key, performed no scoring, and authorizes no
participant launch, merge, Core or Protocol change, Repository authority
action, Standing change, or Decision effect.

## Commit and scope

The review used an isolated clean checkout and refreshed the live remote ref.
The commit, tree, parent, and remote branch matched exactly. The producer diff
adds nine files under the single neutral calibration execution directory and
modifies or deletes no pre-existing byte.

The complete held replacement artifact, the stopped registration, the stopped
run-01 evidence, all Rust crates, and the current Protocol and architecture
documents have the same Git object identities as the F08-passed parent. There
is no participant run evidence and no scientific, packet, prompt, schema,
study-design, scoring-gate, Core, Protocol, Standing, authority, or Decision
change.

## Calibration custody

The eight-file evidence set and all six frozen input bindings recompute from
committed bytes. The custody manifest has root
`sha256:b641951f31d1625fcd842bf290a07e3aeca0fab15396d545989da913bb4ecef8`.

The consumed permit has canonical root
`sha256:28ed87c4d17455d781871b38e632b999760cb67617bb3ac34e9131fcd2107a65`.
After excluding only mutable issuance fields (`status` and `expires_at`), its
identity equals the frozen held calibration template exactly. It is authorized
for `neutral-schemafix-calibration-01`, condition `neutral-calibration`,
participant identity `neutral-schemafix-sol-01`, and attempt 1. The execution
evidence contains exactly one consumed calibration permit and no unconsumed
calibration permit.

The launch binds the consumed permit bytes at
`sha256:475b061ef7c7c52112517b56441bcffc582ee22814c3598eadeb500b598477ba`.
The unchanged runtime performs an atomic rename from the issued permit to the
consumed path before it writes the launch record or starts the provider. The
recorded consumption precedes provider start, and provider completion precedes
permit expiry.

## Exact provider evidence

The event stream contains exactly four events: one thread start, one turn
start, one agent response, and one turn completion. It contains no tool,
command, file-change, search, computer-use, compaction, continuation, or retry
event. The single agent response parses to the exact retained response file.

- provider events bytes:
  `sha256:3893f57e0a3f63dca65a2f64d6574e3cf72481d0b3df0250595d39763e2a8f88`;
- provider stderr bytes: the empty-file root
  `sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`;
- response bytes and canonical response root:
  `sha256:9748c08fee1cc90f44c8c50bfd28e8911edd1ca819a0f0a224e209cb04158372`;
- terminal receipt bytes:
  `sha256:1e592dd1ccfc375c3dca812eda0d3c321b45aa458b60ad2e374c82ddc56aada5`.

The exact recorded usage is 6,765 input tokens, zero cached input tokens, zero
cache-write input tokens, 374 output tokens, and zero reasoning-output tokens.
The process exited zero without timeout after one turn, with zero tools,
compactions, retries, or substitutions.

The response object is byte-equivalent in meaning to the preregistered neutral
valid response and recomputes the configured expected response root. An
independent network-none validation inside the frozen image confirmed that the
same response passes both schemas.

## Runtime and schema bindings

The receipt and result bind the exact F08-passed runtime:

- image and manifest: `sha256:4799bee82c708fb68006b9c558a0fc345a0e7d1f2936fcad298a3d775e0d08bb`;
- image config: `sha256:c0868b563acbd5dd6c80d9512cc1bf019598d3579026fc7fed465cef2d60e56c`;
- complete OCI tar: `sha256:b48f80b60ad1407824e8cd9e1fd3c60abcfd486ed672f8d31ddc9918a28b0b74`;
- runtime root: `sha256:4ec67f446c98848eed59f3a3597a395962c14f3fe2fadcda70a367e9738772ab`;
- runtime source: `sha256:57ef9fda64b3f9cc0fd253d3ea807075e1adb466c42621cd4109bed6e774eafb`;
- registration: `sha256:4d078776f41ffd1df18768791de58014117319f19727c3b9671b606e527c1276`;
- calibration assignment: `sha256:10b8ecc0822312d495ce56af2f5653e0538ec5a413c9e0c9acf51732317b6770`;
- calibration configuration: `sha256:0879e099f9a48ef07be3139933a61c2782b86e9fe95bfec543ae896eaaaee446`;
- registered schema: `sha256:ac96be686e749792956dfa1dfe9560f85c53d55c27fe2e8fd32bcc2a96a634ba`;
- provider derivative: `sha256:896f242086805d3b51e81ed04e6d50f33eb2b7deb71b7a1689e9abeba3b67eaf`;
- prompt: `sha256:eed8ad5804e999b60457ae959b316a5047c1b310661dcb59a4a7460b1292396e`;
- neutral packet: `sha256:4c94831df6a848e1685b47d8d59714064a377d7feb4fdb366450feb5a4491f1c`.

All receipt hashes for the event stream, stderr, response, schemas, prompt,
assignment, configuration, trust bundle, and image match the frozen inputs.

## Teardown and held study state

The launch contract uses an ephemeral `--rm`, read-only container with a
read-only credential mount. No container derived from the calibrated image
remains. The receipt records no retained credential, the retained evidence has
no credential-shaped content, stderr is empty, and both the result and custody
manifest record clean teardown.

The calibration has no denominator credit. The study remains `not_run` at
0/36. Exactly 36 participant permit templates remain `held` with
`expires_at=not_authorized`; no participant consumed-permit file exists.
Recorded protected-adjudication accesses and scoring runs are zero, and the
recorded authority effect is `none`.

## Focused checks and adversaries

The held benchmark verifier, prelaunch custody verifier, 21 benchmark tests,
five provider-runtime tests, Ruff, the JavaScript event-contract tests,
network-none exact response validation, and `git diff --check` pass. Independent
mutations for a duplicate turn event, forbidden tool event, missing terminal
event, permit-identity drift, and expected-response-root drift all fail closed.

## Residual boundary

This PASS confirms only the terminal neutral schema calibration at the exact
producer evidence commit above. It does not release or authorize any participant
permit. Any participant action remains a separate explicitly authorized step
outside this review.
