# Held-out cold-successor task

You are the first successor to receive this packet. Use only the assigned
family packet.

1. Identify the exact predecessor and successor.
2. Classify every listed consequence as `affected`, `unaffected`,
   `must_reassess`, or `presently_unprovable`.
3. Select one closed first-safe-action code for each consequence.
4. Select the correction's closed authority-effect and authority-action codes.
5. Return one exact evidence binding for each listed consequence, containing
   both its packet path and SHA-256 digest.

Return only JSON matching `response-schema.json`. Do not contact anyone, use a
network or tool, issue an acceptance action, or alter authoritative status.
