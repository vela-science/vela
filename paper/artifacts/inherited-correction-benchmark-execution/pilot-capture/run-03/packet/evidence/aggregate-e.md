# Aggregate E evidence note

The two-site aggregate consumes `yield-b` for Site P and the exact adjusted
yield for Site Q. The packet does not contain the Site Q Claim, source bytes, or
content binding. Absence is explicit; it must not be interpreted as zero or as
evidence that the aggregate is unaffected.
