[package]
name = "vela-repository"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Internal durable repository transaction runtime for Vela."
publish = false

[dependencies]
vela-protocol = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
tempfile = { workspace = true }
unicode-normalization = { workspace = true }

# Enables crash injection and recovery inspection only for vela-cli's
# cross-crate authority-transaction integration tests. The runtime's unit
# failpoint suite stays in this crate, and this feature exports no authorization
# implementation.
[features]
test-support = []

[lints]
workspace = true
