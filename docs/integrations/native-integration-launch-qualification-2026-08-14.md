# Native integration launch qualification, 2026-08-14

This report closes the internally controlled Phase 0–4 implementation work for
the Vela Native Repository Integration and Authority Plan. It does not turn
technical completion into adoption, scientific validation, or a new authority.

It is bound to these approved inputs:

- canonical architecture SHA-256
  `3ac5740763db46c2c64a0d2154c6ab464def2cd8371e265d16a9be083f374ead`;
- execution plan SHA-256
  `4e499ed9703560bf8f859a709d4e8f9265980e1a089a4e3fe1427583c6a0836f`;
- strategic launch overlay SHA-256
  `ac23157f8dff2a2ee9ee6bbcc5584effbb830bf0a4c4210dd4e3d233d2cfa9ab`.

## Milestone disposition

| Milestone | Disposition | Reason |
| --- | --- | --- |
| Phase 0–4 internally controlled integration program | complete | Core, both native consumers, Math placement, Web source, Physlib, materials, and bioinformatics packets are implemented, reviewed, tested, committed, and published to their owned repositories. |
| Public launch candidate | wait | The signed CLI is public and the Problem-first Web source is published, but the current Web deployment is externally blocked and the flagship 321 authority transition has not occurred. |
| Protocol 1.0 | wait | External conformant production, independent scientific reading, a real correction cascade, cross-implementation inheritance agreement, and plural authority remain unproved. |
| Protocol breakthrough or established infrastructure | not claimed | No independent adoption, scientific lift, recurrence, or cold-successor improvement is inferred from internal tests or same-operator pilots. |

Vela `v0.975.0` is a pre-1.0 implementation release of the Protocol 1 release
candidate. Native integration remains a package-plane structural contract with
`authority_effect: none`; it is not a Protocol object and does not execute a
Method or change Standing.

## Phase exits

| Phase | Exit evidence |
| --- | --- |
| 0 | ARC-01 and INT-00 are published in Core. The four rooted documents are Manifest, Profile, Binding, and Method; Exact Reference, mapping relation, and translation disposition remain value contracts. The hostile corpus passes. Math is frozen for future generic work and all 927 retained paths are classified without moving authority history. |
| 1 | `lean-proofs` commit `06d1322e62aa28b860da1ec66465d913c1902c78` and Formal Conjectures fork commit `3add37729550480fea34f67690ec708c549f524b` are published source-owned integrations. Their cold consumers require neither `vela-science/math`, authority credentials, hosted Vela, mutable latest references, nor private maintainer context. |
| 2 | Core commit `bea4ec2af0772e366a0670d49a10b7085a4c73c1` supplies structural `integration check` and `integration inspect`. Core added 479 lines while the two maintained consumers deleted 480, for net minus one. No scientific Profile moved to Core. The stable contract is recorded by commit `47c4c8a55ecc917847462f51ac848b9272107350`. |
| 3 | Math commit `e68ee391b54fc86b4bd5abf65215cdd4de8fafdc` keeps and reduces the bounded Vela Mathematics Program without rewriting history; strict replay remains 3 accepted, 0 pending, Repository root `sha256:ae41be4a91265d91967344459fa12583314ec05c5a0ebc74d8b0136195879511`. Web commit `94144a72a9635b882ba541c420aa9475c86fae91` implements rooted occurrence joins and Problem-specific Claim provenance without a schema or authority mutation. The 321 authority action and production deployment remain explicit waits below. |
| 4 | Physlib fork commit `ec4d167678020bd2c768a8b03c605f44462f24d0`, AiiDA/OPTIMADE commit `c767fa7b62af4463d562a98c39e8b5481857cdd6`, and Nextflow/RO-Crate commit `5e9bd8afde3342201afaeaf6c5b241adfcee0afa` are published bounded stress tests. They establish neither upstream adoption nor a universal scientific ontology, registry, runtime, or new field authority. |

## Signed CLI release

Vela `v0.975.0` is published at exact commit
`8433d7cfa61f07bff2d9e6a792c77b2b220a65bd`, tree
`4a427b1d2eadf95982d61911080ab64edc90d831`, and annotated tag object
`1ed993125e0f74e527aee2f04968ee961cabd192`.

The release workflow retained Linux x86-64 and macOS Apple silicon archives,
checksums, SBOMs, GitHub build attestations, and signed release manifests. Both
provider-independent distribution signatures verify under
`release@vela.space`, fingerprint
`SHA256:MX3Eo1o9S5pLnx2kiNyAy2aME7PAWDtvqtUBljJst1M`. A disposable empty-environment
install requiring the signed manifest passed, and the installed binary exposes
`integration check` and `integration inspect`.

Independent review found and blocked an initial candidate that retained 92
absolute Cargo registry paths. The published binary remaps Cargo and account
homes to stable public prefixes and contains no builder account, Cargo, source,
temporary, or target path. The macOS archive is portable but is not Apple
Developer ID notarized.

## Flagship mathematics flow

Erdős 321 is the single flagship. The published source and product packets now
connect its native occurrences, statement variants, pinned artifacts and
environments, mechanical checks, statement-fidelity review, Repository-local
Standing, correction plan, and Web resolver without collapsing their owners.

The existing accepted predecessor is
`vcl_3d4fd59554ccaa2b792b08abae16a8d0fe329d4901ad798fe05c6c7769c9966b`,
root
`sha256:d5d77e7d96e390e0bf692d0abd44367eb06a0c6a61534e1c6654962d6c644776`.
Math retains an exact proposed revision with relation `corrects`, replacing the
shortened source revision and omitted rooted occurrence identities. No
successor Claim, Proposal, Verification, inbox entry, Decision, Event, or
Standing change has been fabricated. The remaining authorized sequence is:

`Submission → two scoped Verifications → exact inbox entry → Decision → Event and replay → Web remap`.

Erdős 887 remains the fail-closed counterexample. Formal Conjectures has no
reviewed resolver mapping for it, so Web exposes no canonical 887 Problem route
and does not substitute number equality, CI, review, merge, or publication for
occurrence identity or Standing.

## problems.science qualification

The source at Web commit `94144a72a9635b882ba541c420aa9475c86fae91`
passes the full local check, lint, test, build, authority-boundary, route,
public-output, and patch-hygiene gates. The data package passed 460 tests with 9
declared skips; the Observatory application passed 492 tests.

Browser qualification of that exact local build found:

- the Problem 321 page became readable in 507 ms in the local production build;
- its opening order is Problem, Current State, Local Claims, source facts and
  coverage, Checks, Research Brief, Decisions, correction history, then next
  Targets; roots and advanced protocol records remain under Exact provenance;
- source status `solved` and local Standing `Not assessed locally` are visibly
  separate, and the absent corrected Claim is stated rather than inferred;
- 390 × 844 mobile rendering has no horizontal document overflow;
- keyboard focus traverses the breadcrumb, search, notifications, appearance,
  state/work modes, ledger, and source links without a trap;
- 200% scaling, reduced motion, forced colors, print, touch controls, and browser
  console checks pass;
- signed-out localhost behavior refuses account access, while the current owned
  production account surface shows an authenticated human identity with
  `Authority: None` and explicitly says it is not a Vela actor ID;
- `/problems/erdos-problems/887` returns a truthful 404 with no substituted
  Repository state.

This is exact-source and local-browser evidence. It is not evidence that the new
commit is live. The current production manifest still identifies commit
`7e84cfe521db47b918d90ae93e88be273a2953b6`, deployment
`dpl_CthHrnmvVtq6nG954J6mXkPe3QmK`, and projection root
`sha256:5c0df33530097a06a3be49cc26eb79fa65d3db5e9bc9aa7c89ecc646ec95256b`.

## Appendix A disposition

### Protocol and CLI

The internal-consistency, wire-stability, multi-language conformance,
authority-vector, supported-install, signed-asset, provider-loss, exact-authority,
and no-normative-Web gates pass. Provider-loss reconstruction is retained for an
earlier signed release; no latest-release provider-loss exercise is claimed.

### Mathematics

The flagship selection, exact occurrences and variants, pinned native material,
mechanical-versus-fidelity separation, bounded Math Repository, and prepared
correction path pass. A reader can inspect existing Decisions and replay, but
the new 321 correction Decision, its visible consequence, and its exact remaining
obligations wait on Repository authority.

### problems.science

Problem-first source, distinct source/Standing axes, navigable evidence/check/
Decision/correction surfaces, unavailable-state refusal, local responsive and
keyboard qualification, exact source roots, read-only scientific-state boundary,
and visible reproduction paths pass. Current live-commit qualification waits on
the deployment item below.

The Appendix A cold-visitor comprehension gate remains **wait / not measured**.
The 507 ms render measurement and browser accessibility checks establish page
readiness and interaction behavior; they do not establish that a first-time
researcher understands the product within 30 seconds.

### External validation

The invitation and bounded integration packets are published, but the required
separately operated workbench, independently controlled verifier, maintained
clean-room scientific reader, and independently governed authority have not
completed the flow. Same-program agent review is attributed independent review
of implementation packets, not external scientific governance.

### Communication

The ten- and thirty-second explanations and the GitHub, read-only, and Lean
boundaries are stable in current documentation. Launch claims and nonclaims are
explicit. Public launch, Protocol 1.0, and breakthrough remain separate
milestones.

## External-wait ledger

1. **Web production workflow.** GitHub Actions runs
   `31783131816` (CI) and `31783131769` (projection refresh and deploy) failed
   before checkout. GitHub reported failed recent payments or an insufficient
   Actions spending limit for the private repository. Reconstruction run
   `31783146756` was therefore skipped. The governed release ordering requires
   the rooted projection refresh before the Vercel deploy; no direct stale-
   projection deploy was substituted.
2. **Math authority.** The 321 correction requires authenticated Submission,
   its two scoped Verifications, and an exact authorized Decision under the
   existing Math policy. The authority exists and remains unchanged; the
   authenticated records, inbox entry, authorized Decision, and confirmed signer
   availability have not occurred. This deferred authority action is local to
   the package and did not stop the rest of the program.
3. **Independent validation and Protocol 1.0.** Separately operated production,
   scientific reading, a real dependency cascade, cross-implementation
   inheritance agreement, plural authority, provider-baseline comparison, and
   measured cold-successor improvement remain external evidence requirements.

Erdős 887 resolver equivalence is **not claimed**, not awaited. With 321 as the
flagship, the absent mapping is an intentional unsupported case. Formal
Conjectures or its upstream maintainers owe no adoption or mapping; Web continues
to refuse a canonical 887 route unless source-owned reviewed evidence appears.

Human and agent Activities are peer provenance forms throughout this report.
Actor kind grants neither evidentiary quality nor authority; exact Method,
inputs, outputs, declared independence, principal, policy, and signature remain
separate.
