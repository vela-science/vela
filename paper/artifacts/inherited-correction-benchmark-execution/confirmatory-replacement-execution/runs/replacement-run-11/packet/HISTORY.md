# Git history note

`calibration-a-v2` supersedes
`calibration-a-v1` because
The v1 calculation omitted the documented 1.2 temperature compensation factor. The predecessor asserted
“The instrument scale factor is 10 units per count.” The successor asserts
“The instrument scale factor is 12 units per count.”

This is a candidate-visible research fixture, not a Decision. Standing effect:
`none`. Decision present:
`false`. Scientific validation
present: `false`.
