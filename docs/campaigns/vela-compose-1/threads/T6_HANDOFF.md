# T6 — Cumulative handoff experiment worker contract

Status: blocked on qualified T3 apparatus plus at least one vertical and an S0
preregistration commit.

Question: does explicit governed Standing reduce continuation cost/error versus
competent reconstruction from ordinary artifacts?

Freeze before execution:

- R: ordinary artifacts, not deliberately crippled;
- V: native Vela Standing and resolvable governed context;
- E: strongly preferred information-equivalent neutral structured export.

Use the same downstream model, tools, continuation task, sealed target, and
matched resources. Measure pre-action tokens/time/reads/searches/tool calls,
duplicate work, invalid state assumptions, provenance mistakes, verifier
failures, first valid contribution, and verified downstream progress.

Interpret without rescue: `V ~= E > R` is structured-compilation value; `V > E
> R` isolates systems value from Vela semantics; `V ~= E ~= R` is no shown
advantage; `R > V` is evidence of friction or harmful compression. This is not
ACQUIRE and makes no cognition claim.

Run Lean apparatus qualification first; real-science cells only after dynamic
range. Preserve every cell/exclusion and stop on a qualifying anomaly. Report
preregistration, cells, full table, exclusions, interpretation, and artifacts.

