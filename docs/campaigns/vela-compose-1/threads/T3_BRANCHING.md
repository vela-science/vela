# T3 — Counterfactual branching + metering worker contract

Status: blocked until T1/T2 qualification and an S0 launch decision.

Build source-owned controlled branching from identical governed Standing. Do
not add a disconnected experiment framework or run a foundational experiment.
Use Git and current CLI grammar unless two maintained consumers prove a missing
Core primitive.

Prove identical root digest, branch lineage, branch-local writes, zero
cross-branch contamination, sealed evaluations, deterministic comparison, and
honest metering of model calls/tokens, tools, verifiers, solvers/simulations,
wall/CPU/GPU time, external services, artifacts, persistent state, and human
interventions where technically reliable. Record incomparable metrics as such.

Finish on `campaign/compose1-counterfactual` with design, commands/API, tests,
results, limitations, and commit. Do not merge or start an experiment.

