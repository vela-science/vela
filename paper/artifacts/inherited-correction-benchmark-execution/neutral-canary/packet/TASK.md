# Neutral one-response calibration

Using only the virtual files in this prompt, return the identifiers in
lexicographic order, the sum of their integer measurements, and the exact
SHA-256 binding written in `records/binding.txt`.

Return exactly one JSON object matching `response-schema.json`. Do not request
or call any tool, continue a session, or use outside information.
