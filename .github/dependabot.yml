version: 2

# One mechanism, not two. Renovate would need a GitHub App installed on the
# vela-science org; Dependabot is native and already covers every ecosystem this
# workspace has. There is deliberately no renovate.json beside this file.
#
# Every ecosystem below waits a week before it will propose a release.
# `default-days` is the cooldown key that applies to all of them — the semver-*
# keys only reach package managers that carry semver — and a week is roughly the
# window in which a compromised publish gets noticed and yanked. A SHA pin
# records which bytes run, not that those bytes were ever worth running.
# Groups carry minor and patch only, so a major sits in its own pull request.
# The first cargo group proposed ten crates at once, three of them major —
# sha2 0.10 to 0.11 and ed25519-dalek 2 to 3 — which are the
# crates that produce canonical roots and sign authority envelopes. A hash or
# signature implementation change has to be verified against retained records
# deliberately, and bundling it behind a clap patch means either the safe bump
# waits on the breaking one or the breaking one rides in on the safe one's
# review.
updates:
  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "09:00"
      timezone: America/Toronto
    open-pull-requests-limit: 2
    cooldown:
      default-days: 7
    # `cedar-policy` used to be ignored here, because its version was named
    # inside every policy bundle the workspace minted and a bump invalidated
    # signatures already written. The evaluator is Vela's own now and there is
    # no engine version on the wire, so there is nothing left to pin.
    groups:
      cargo:
        patterns:
          - "*"
        update-types:
          - minor
          - patch

  # conformance/ and packages/vela-source-manifest/ each carry their own
  # uv.lock. CI runs `uv run --locked`, so a manifest bump that misses the lock
  # fails the run instead of landing quietly.
  - package-ecosystem: uv
    directories:
      - /conformance
      - /packages/vela-source-manifest
    schedule:
      interval: weekly
      day: monday
      time: "09:00"
      timezone: America/Toronto
    open-pull-requests-limit: 2
    cooldown:
      default-days: 7
    groups:
      python:
        patterns:
          - "*"
        update-types:
          - minor
          - patch

  # Every action in this repository is pinned to a full commit SHA. Dependabot
  # rewrites the SHA in place, which is the only way these pins stay current
  # without someone resolving tags by hand.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "09:00"
      timezone: America/Toronto
    open-pull-requests-limit: 2
    cooldown:
      default-days: 7
    groups:
      workflow-actions:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
