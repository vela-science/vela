{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "bun run scripts/build-fallback-snapshot.ts",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "bun run scripts/build-fallback-snapshot.ts && astro build && flyctl deploy"
  },
  "dependencies": {
    "astro": "^6.1.9"
  }
}