name: release

# Build the `vela` binary for the three platforms install.sh knows about
# and attach each one (plus a .sha256 companion) to the GitHub release for
# the pushed tag.
#
# Asset names MUST match install.sh's case arms exactly:
#   darwin-arm64|darwin-aarch64 -> vela-macos-aarch64
#   linux-x86_64                -> vela-linux-x86_64
# Intel macOS (x86_64) is not built: GitHub's macos-13 runners are being sunset
# and Apple Silicon is the macOS baseline. install.sh tells Intel users to build
# from source.
#
# install.sh downloads "<asset>.sha256" but saves the binary locally as
# "vela" and runs `cd "$TMP"; shasum -a 256 -c vela.sha256`, so the checksum
# file's content must reference the filename "vela" (a line of the form
# "<hash>  vela"), NOT the asset name. We produce it by renaming the built
# binary to "vela" and running `shasum -a 256 vela > vela-<target>.sha256`.

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write        # OIDC for keyless Sigstore signing
  attestations: write    # SLSA build provenance on the real assets

jobs:
  build:
    name: build ${{ matrix.asset }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - asset: vela-macos-aarch64
            runner: macos-14
            target: aarch64-apple-darwin
          - asset: vela-linux-x86_64
            runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Verify tag matches workspace version
        shell: bash
        run: |
          set -euo pipefail
          workspace_version="$(awk '$1 == "version" && $2 == "=" {gsub(/\"/, "", $3); print $3; exit}' Cargo.toml)"
          expected_tag="v${workspace_version}"
          if [ "$GITHUB_REF_NAME" != "$expected_tag" ]; then
            echo "tag $GITHUB_REF_NAME does not match workspace version $workspace_version" >&2
            exit 1
          fi

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build vela (release)
        run: cargo build --locked --release --bin vela --target ${{ matrix.target }}

      - name: Stage asset + checksum
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          # Rename the built binary to "vela" so the checksum line reads
          # "<hash>  vela" — exactly what install.sh verifies in its tmp dir.
          cp "target/${{ matrix.target }}/release/vela" "vela"
          chmod +x "vela"
          shasum -a 256 "vela" > "${{ matrix.asset }}.sha256"
          mv "vela" "dist/${{ matrix.asset }}"
          mv "${{ matrix.asset }}.sha256" "dist/${{ matrix.asset }}.sha256"
          echo "--- ${{ matrix.asset }}.sha256 ---"
          cat "dist/${{ matrix.asset }}.sha256"

      # SLSA build provenance, recorded in the Sigstore transparency log,
      # on the EXACT bytes users download. Verify before trusting:
      #   gh attestation verify <file> --repo vela-science/vela
      - name: Attest build provenance
        uses: actions/attest-build-provenance@v1
        with:
          subject-path: dist/${{ matrix.asset }}

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            dist/${{ matrix.asset }}
            dist/${{ matrix.asset }}.sha256
