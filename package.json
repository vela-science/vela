{
  "name": "@vela-science/workspace",
  "private": true,
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/protocol build",
    "typecheck": "bun run --cwd packages/protocol typecheck && bun run --cwd packages/protocol build",
    "test": "bun run --cwd packages/protocol test",
    "test:benchmarks": "python3 -m unittest discover -s benchmarks/product-compression -p 'test_*.py'",
    "check": "bun run typecheck && bun run test && bun run test:benchmarks"
  }
}
